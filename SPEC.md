# 單字練習網站 — 完整技術規格（給 Claude Code 實作用）

> **實作進度更新（2026-09-01）**：已完成下列項目，本文件其餘章節已同步更新：
> - 老師的 Excel（`★每日單字_單字卡+中翻英+英翻中.xlsx`，1316 個單字、題號 1~1316
>   連續無缺漏）已轉出成 `words-data.js`（前端本機示範用）與 `單字庫.csv`
>   （匯入 Google Sheets 用）。**目前先只有一個單字庫**，不是原本規劃的
>   2000 單／1200 單兩份（之後有第二份資料再加即可，架構不用改）。
> - **克漏字題型已移除**（Excel 沒有例句欄位），只保留中文卷／英文卷。
> - 中文卷批改會先去除詞性標籤與括號註記（如「(n)」「(vt)」）再比對答案，
>   細節見 `app.js` 的 `acceptedZhAnswers()`。
> - Google Apps Script 後端程式碼已寫好，見 `Code.gs`，可直接貼到 Apps Script 專案。
> - `app.js` 已改為呼叫 `APPS_SCRIPT_URL`；网址尚未部署設定前，會自動退回
>   「本機示範模式」（demo／1234，資料存 localStorage），方便先用真實單字預覽畫面。
> - 尚未完成：實際建立 Google Sheets 試算表、部署 Apps Script、部署 GitHub Pages
>   （見第 7 節「部署步驟」，這幾步需要老師的 Google 帳號操作，Claude Code 無法代勞）。
>
> **實作進度更新（第二輪）：計分機制**：已加入累計分數功能，規則見第 4.1 節。
> 「學生」工作表需要新增一欄「累計分數」（見第 3.2 節），`Code.gs`／`app.js` 都已更新。
>
> **實作進度更新（2026-09-05，第三輪）**：
> - `Code.gs` 新增 `getProgress` action，回傳這個帳號「答對過」的題號（不分中文卷／
>   英文卷，對過一次就算精熟，見 `handleGetProgress`）；`getAssignment` 回應也多帶
>   `dailyCount`／`programStartNum`／`wordBankMax` 三個欄位，讓前端能自己算出任何一週
>   （不限本週）的題號範圍，不用每翻一週就多打一次 API。**這是本次唯一動到 `Code.gs`
>   的地方，需要老師重新到 Apps Script 貼上新版並「部署 → 新版本」才會生效**（否則
>   前端會抓不到這些新欄位，本週進度會顯示不出正確範圍）。
> - 首頁「本週進度」升級：可左右翻頁看上一週／下一週／未來更多週（範圍算好、單字內容
>   則用 `words-data.js` 內建的完整題庫切，不用額外打 API），每天會顯示「✓ 已完成」
>   （這天的字，不分中文卷／英文卷，全部都對過一次）或「尚未完成」（已經過去卻還沒
>   完成）；並在首頁加了一條進度提示 bar，本週有過去的天沒完成會催促、有提前做完
>   未來的天會鼓勵（只看「目前實際那一週」，不含老師／學生自己翻看的其他週）。
>   **完成的定義是我自行判斷「無論中文、英文」的意思＝兩種測驗卷任一種對過就算**，
>   不是要求中文卷跟英文卷都要各對一次；如果老師的原意是兩種都要對過，請告訴 Claude
>   Code 再調整 `isRangeMastered()` 的判斷條件。
> - 登入按鈕、測驗送出答案都加上明確的「處理中」鎖住畫面（登入中／批改中，轉圈動畫＋
>   蓋住整張卡片擋掉誤觸），並加了 400ms 防連點，避免連按 Enter 或雙擊把下一題的
>   輸入框直接當空白答案送出。
> 這份文件是給 AI 開發助理（Claude Code）看的工程規格。目的：把現有的前端雛形
> 接上真正的 Google Sheets 資料，並部署到 GitHub Pages。
> 前端雛形（`index.html` / `style.css` / `app.js`）已經做好、畫面與互動邏輯都對，
> **請勿更動 UI 版面與 CSS**，主要工作是：
> 1. 建立 Google Apps Script 後端
> 2. 把 `app.js` 裡標記 `TODO 接後端` 的假資料函式，換成呼叫後端 API
> 3. 協助設定 GitHub Pages 部署

---

## 1. 專案目標

國中升高中英語單字背誦與測驗網站，手機／iPad／電腦皆須正常顯示。

- 老師在 Google Sheets 管理單字庫、學生帳密、指派範圍
- 學生用帳號密碼登入，每天背 10 個單字並測驗
- 週一到週六：每天新的 10 個單字（範圍每週自動往後接續，不用老師手動改）
- 週日：把這週全部單字（60 個）混合，先看過一輪再測驗
- 兩種題型：中文卷（看英打中）、英文卷（看中打英）
  （原規劃還有克漏字例句選字，因目前單字資料沒有例句欄位，先不做）
- 答錯要記錄進「錯題本」，可針對錯題重測
- 所有作答紀錄要寫進 Google Sheets，讓老師能查看每個學生的細節
- 靜態網站部署在 GitHub Pages；後端用 Google Apps Script（不租主機、不用資料庫費用）

---

## 2. 系統架構

```
瀏覽器 (GitHub Pages 上的 index.html/app.js)
        │ fetch(APPS_SCRIPT_URL, { method:"POST", body: JSON })
        ▼
Google Apps Script Web App（doPost 進行路由分派）
        │ 讀寫
        ▼
Google Sheets（同一份試算表，多個工作表）
```

- Apps Script 需部署成 **Web App**，存取權限設「知道連結的任何人」，執行身分設「我」（Owner）。
- 前端沒有任何 Google 帳密，所有存取都經過 Apps Script 這一層。
- 全部溝通走單一 `POST` 端點，用 body 裡的 `action` 欄位區分要做什麼（見第 5 節），
  這樣可以避開 Apps Script Web App 對 GET query string 與 CORS 的一些限制。

---

## 3. Google Sheets 資料結構

同一份 Google 試算表裡，需要以下工作表（sheet tab）。**表頭列請完全比照下列欄位名稱**，
Apps Script 程式會用欄位名稱（非欄位英文代號）去對照。

### 3.1 單字庫
工作表名稱：`單字庫`（`單字庫.csv` 可直接匯入，共 1316 題）

| 欄位 | 型態 | 說明 |
|---|---|---|
| 題號 | 整數，從 1 開始連續 | 之後所有指派範圍都用這個題號 |
| 英文 | 文字 | |
| 中文 | 文字 | 保留原始格式，可能含詞性標籤如「(n)」「(vt)」、多義用「、」分隔（例如「真相、真理、真實性(n)」）。批改時由程式自動去除括號標籤後再比對，只要對到其中一個意思就算對 |

> 之後若老師有第二份單字資料（例如原規劃的「2000單」），可以另建一個同格式的工作表，
> 並在 3.2「學生」表新增一個「單字庫」欄位指到對應工作表名稱即可，目前先不用。

### 3.2 學生名單
工作表名稱：`學生`

| 欄位 | 型態 | 說明 |
|---|---|---|
| 帳號 | 文字，唯一 | |
| 密碼 | 文字 | 純文字明碼即可（小系統、學生少，不做雜湊） |
| 姓名 | 文字 | |
| 起始題號 | 整數 | 這門課「第一週」的起始題號 |
| 每週題數 | 整數，預設 10 | 每天背幾個字（週一到週六共 6 天） |
| 開課週一日期 | 日期，格式 YYYY-MM-DD | 用來算「現在是第幾週」 |
| 累計分數 | 數字，初始填 0 | 見第 4.1 節計分規則，由系統自動累加，不用老師手動改 |

**自動接續公式**（Apps Script 端計算，不要存在 Sheet 裡，每次即時算）：

```
今天 = new Date()
開課週一 = 學生列.開課週一日期
經過天數 = floor((今天 - 開課週一) / 一天的毫秒數)
週次索引 weekIndex = floor(經過天數 / 7)   // 從 0 開始
本週起始題號 = 學生.起始題號 + weekIndex * (學生.每週題數 * 6)
本週結束題號 = 本週起始題號 + (學生.每週題數 * 6) - 1

今天星期幾 weekday：1=週一 ... 6=週六 7=週日（週日=經過天數 % 7 == 6 那一天）
若 weekday 為 1~6：
    今日起始題號 = 本週起始題號 + (weekday-1) * 學生.每週題數
    今日結束題號 = 今日起始題號 + 學生.每週題數 - 1
若 weekday 為 7（週日）：
    今日題目 = 本週起始題號 ~ 本週結束題號 全部（混合出題）
```

> 注意：`每週題數` 是「每天」的題數（目前預設 10），不是一週總數；一週總數 = 每週題數 × 6。
> 這個命名容易搞混，Claude Code 實作時請用程式變數 `dailyCount` 取代，Sheet 欄位名稱維持中文即可。

### 3.3 作答紀錄
工作表名稱：`作答紀錄`（每答一題新增一列，不要更新舊列）

| 欄位 | 型態 |
|---|---|
| 時間 | 系統時間戳（ISO 字串或 Sheets 日期時間） |
| 帳號 | 文字 |
| 題號 | 整數 |
| 英文 | 文字 |
| 中文 | 文字 |
| 測驗類型 | `中文卷` / `英文卷` |
| 學生作答 | 文字 |
| 是否正確 | TRUE / FALSE |

### 3.4 錯題本
工作表名稱：`錯題本`（用「帳號＋題號＋測驗類型」當唯一鍵，累加次數，而不是每次都新增一列）

| 欄位 | 型態 |
|---|---|
| 帳號 | 文字 |
| 題號 | 整數 |
| 英文 | 文字 |
| 中文 | 文字 |
| 測驗類型 | `中文卷` / `英文卷` |
| 累計錯誤次數 | 整數 |
| 最後錯誤時間 | 時間戳 |

規則：
- 答錯 → 若該鍵已存在，累計錯誤次數 +1、更新最後錯誤時間；若不存在，新增一列，次數=1。
- 答對 → 若該鍵存在於錯題本，**整列刪除**（代表已訂正過關）。

---

## 4. 答案批改規則（已確認，請照此實作）

- **英文卷**：學生輸入去除頭尾空白、轉小寫後，與正確英文（同樣去空白轉小寫）完全相同才算對。不接受其他寬鬆比對。
- **中文卷**：正確答案欄位先去除所有括號內容（詞性標籤如「(n)」「(vt)」，或其他括號註記），
  再用「、」「,」「，」「;」「；」與空白切成多個可接受答案，學生輸入（去頭尾空白）
  等於其中任何一個即算對。例如「真相、真理、真實性(n)」的可接受答案是
  「真相」「真理」「真實性」三者之一。

以上邏輯**前端 `app.js` 裡已經寫好**（`checkAnswer()` / `acceptedZhAnswers()` 函式），Apps Script 那邊如果也要做重複驗證（防止有人繞過前端直接打 API），批改邏輯要跟前端保持一致。

### 4.1 計分規則（已確認，請照此實作）

- 同一個帳號、同一個題號、同一種測驗類型（中文卷／英文卷分開計），**只有「有史以來
  第一次作答」會計分**：答對 +1 分、答錯 -0.5 分。
- 判斷「是不是第一次」的方式：看「作答紀錄」工作表裡有沒有這個帳號＋題號＋測驗類型
  的舊紀錄。有紀錄（不管當初對或錯）就代表不是第一次，這次不計分。
- 不計分的情況包含：錯題本重測、週日整週複習又碰到本週已經練過的字、同一天內
  重複測驗同樣的字。這些狀況**仍然要寫入作答紀錄、仍然要更新錯題本**，只是不影響分數。
- 分數是累加的（`學生` 工作表的「累計分數」欄），不會每天重算或歸零，讓學生可以
  一天天把分數往上疊。
- 分數計算實際發生在 `submitAnswer` 這個 API（見 5.3 節），由後端判斷、後端寫入，
  前端只負責顯示（`app.js` 的本機示範模式也用同一套邏輯，方便沒接後端時先測試）。

---

## 5. Apps Script API 規格

全部走同一個 Web App URL，用 `POST`，body 是 JSON，用 `action` 欄位分派。

### 5.1 登入
```json
// 請求
{ "action": "login", "account": "demo", "password": "1234" }

// 成功回應
{ "ok": true, "name": "王小明", "account": "demo" }

// 失敗回應
{ "ok": false, "error": "帳號或密碼錯誤" }
```

### 5.2 取得指派與今日/本週單字
```json
// 請求
{ "action": "getAssignment", "account": "demo" }

// 回應
{
  "ok": true,
  "weekIndex": 0,
  "weekStart": 1,
  "weekEnd": 60,
  "weekday": 2,               // 1~7，7=週日
  "todayStart": 11,
  "todayEnd": 20,
  "todayWords": [ { "id": 11, "en": "after", "zh": "在...之後" }, ... ],
  "weekWords": [ /* 只有週日或前端要顯示整週總覽時才需要帶，其餘可省略以減少流量 */ ],
  "score": 12.5              // 該學生目前累計分數
}
```

### 5.3 送出一題作答
```json
// 請求
{
  "action": "submitAnswer",
  "account": "demo",
  "wordId": 11,
  "en": "after",
  "zh": "在...之後",
  "mode": "zh",              // "zh" | "en"
  "given": "在之後",
  "correct": true
}

// 回應
{
  "ok": true,
  "firstAttempt": true,     // 這是不是這個帳號對這題這種測驗類型的第一次作答
  "delta": 1,                // 這次得分變化：+1 / -0.5 / 0（非第一次作答）
  "newScore": 13.5           // 更新後的累計分數；非第一次作答時為 null
}
```
> 批改本身在前端做（維持現有 `checkAnswer()` 邏輯），送出時已經帶著 `correct` 結果，
> Apps Script 收到後負責寫入「作答紀錄」、更新「錯題本」，並依 4.1 節規則判斷是否為
> 第一次作答、需不需要更新「學生」工作表的累計分數（如果要加防呆重新驗證批改結果也可以，但不是必要需求）。

### 5.4 取得錯題本
```json
// 請求
{ "action": "getWrongBank", "account": "demo" }

// 回應
{
  "ok": true,
  "items": [
    { "wordId": 5, "en": "active", "zh": "活躍的", "mode": "zh", "count": 2 }
  ]
}
```

---

## 6. 前端目前狀態（已完成）

`app.js` 已經改好，`callApi()` 統一用 `fetch(APPS_SCRIPT_URL, {method:"POST", body: JSON.stringify({action, ...})})`
呼叫後端；`APPS_SCRIPT_URL` 是檔案最上面的常數：

```js
const APPS_SCRIPT_URL = "";  // 部署好 Apps Script 後貼在這裡
```

**只要這個常數還是空字串，前端就會自動走「本機示範模式」**（`demoLogin`/`demoGetAssignment`/
`demoSubmitAnswer`/`demoGetWrongBank`，資料存在 localStorage，帳號 `demo`／密碼 `1234`），
方便在還沒部署 Google Sheets 之前，直接用真實的 1316 個單字預覽整個流程。等
`APPS_SCRIPT_URL` 填上真正網址，就會改成呼叫 Apps Script、示範控制列也會自動隱藏。

送出請求用 `Content-Type: text/plain;charset=utf-8`（而不是 `application/json`），
這是刻意的：Apps Script Web App 對帶自訂 header 的 `application/json` 請求會觸發瀏覽器的
CORS preflight（`OPTIONS`），但 Apps Script 不會正確回應 preflight，導致跨網域呼叫失敗；
用 `text/plain` 可以避開 preflight，`e.postData.contents` 在 Apps Script 端一樣能正常
`JSON.parse()`。**如果之後要改寫呼叫方式，請保留這個 header 設定**，不要改成
`application/json`，否則手機瀏覽器打 GitHub Pages 網址時會打不到 API。

---

## 7. 部署步驟（尚待老師操作，Claude Code 無法代勞這幾步）

1. **Google Sheets**：新增一份試算表，依第 3 節建立 4 個工作表（單字庫／學生／作答紀錄／錯題本），
   表頭列照抄。「單字庫」分頁可以直接把 `單字庫.csv` 匯入（檔案 → 匯入 → 上傳，選擇「取代工作表」
   或「插入新工作表」皆可，之後把分頁改名成「單字庫」）。「學生」分頁至少建一列示範資料，例如
   帳號 `demo`、密碼 `1234`、起始題號 `1`、每週題數 `10`、開課週一日期填最近一個週一、
   累計分數填 `0`。
2. **Apps Script**：該試算表「擴充功能 → Apps Script」開啟編輯器，把 `Code.gs` 的內容整個貼進去
   （取代預設內容），存檔。「部署 → 新增部署作業 → 網頁應用程式」，執行身分選「我」、
   存取權選「知道連結的任何人」，部署後複製 `.../exec` 網址。
3. **前端**：把該網址貼進 `app.js` 最上面的 `APPS_SCRIPT_URL`，本機直接雙擊 `index.html`
   打開瀏覽器測試整個登入／背單字／測驗／錯題本流程能不能正常打到 API。
4. **GitHub Pages**：把 `index.html` / `style.css` / `app.js` / `words-data.js` 推到一個
   GitHub repo，到 repo 設定 → Pages，選擇要發佈的分支（通常是 `main` 根目錄），存檔後會
   得到 `https://<帳號>.github.io/<repo>/` 的網址，用手機和 iPad 實際打開測試（尤其注意
   Safari／行動網路環境下 API 是否正常，行動瀏覽器對 CORS 比桌機嚴格）。

---

## 8. 已經確定的需求（不用再跟老師確認）

- 範圍指派：**自動接續**上一週，不用老師每週手動填新範圍。
- 不需要老師登入介面，老師直接在 Google Sheet 手動維護「學生」工作表。
- 中文答案比對：先去除詞性標籤／括號註記，其中一個意思對即算對（用「、」分隔多個答案）。
- 英文答案比對：不分大小寫、自動去頭尾空白，其餘要求完全打對。
- 題型共兩種：中文卷、英文卷（克漏字因無例句資料，先不做）。
- 單字庫目前只有一份（Excel 轉出的 1316 字），不是原規劃的 2000＋1200 兩份。

## 9. 尚待確認 / 之後可能要追加的事項

- 若要恢復克漏字題型，需要幫全部 1316 字準備例句（可請 Claude 分批生成，或老師自行編寫），
  再於「單字庫」分頁加回「例句」欄位、Code.gs 與 app.js 加回對應邏輯。
- 若之後有第二份單字資料（例如「2000單」），可依 SPEC.md 3.1 的說明另建一個工作表，
  並在「學生」表加回「單字庫」欄位指定學生對應哪一份。
- 目前沒有「老師總覽報表」頁面，只有 Google Sheet 原始資料；如果老師希望有一個
  總覽儀表板（例如每個學生本週正確率），屬於後續可以再加的功能，不在這次範圍內。
- 密碼目前是明碼存在 Sheet 裡，對小班教學來說風險可接受；如果之後學生變多、需要更嚴謹的
  驗證機制，可以再討論改進方式。

---

## 10. 驗收測試清單

- [ ] 用學生帳密可以登入，密碼錯誤會顯示錯誤訊息
- [ ] 週一到週六，首頁顯示的「今日範圍」題號正確對應星期幾
- [ ] 過了一週之後（或修改試算表的開課日期往前推），範圍會自動往後接續，不用手動改
- [ ] 週日登入，首頁顯示「本週複習」，可以看到整週單字並用整週單字出題
- [ ] 中文卷：看英文輸入中文，其中一個正確答案對即算對（詞性標籤不用打）
- [ ] 英文卷：看中文輸入英文，大小寫不分、去空白後比對
- [ ] 答錯的題目出現在錯題本，累計次數會增加；答對後從錯題本移除
- [ ] 錯題本可以針對錯題重新測驗（中文卷／英文卷都可選）
- [ ] 一個字第一次作答：答對加 1 分、答錯扣 0.5 分，畫面右上角分數即時更新
- [ ] 同一個字用錯題本重測，或週日整週複習又碰到本週練過的字：分數不會再變動
- [ ] 重新整理頁面或重新登入，累計分數還在（代表確實寫進 Google Sheets 的「學生」工作表）
- [ ] Google Sheets 的「作答紀錄」工作表即時看得到新的作答列
- [ ] 手機直式、iPad 橫向/直向，畫面都不跑版、按鈕都點得到
